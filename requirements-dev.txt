# Dev/test dependencies.
#
# Install into whichever interpreter runs the suite:
#
#     python -m pip install -r requirements-dev.txt
#     python -m pytest tests -q
#
# Note: the bundled `venv/` (if present) may not have pytest installed. Any
# Python 3.12 with these packages works -- tests/conftest.py puts the repo root
# on sys.path so `import agentgraph` resolves to the vendored copy here.

pytest>=8.0

requests  # HTTP client used by the live-server tests
ruff      # lint + format; configured in pyproject.toml
